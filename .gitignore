# Node modules
node_modules/            # Ignores the node_modules directory for Node.js projects.

# Python bytecode
__pycache__/             # Ignores the __pycache__ directory generated by Python.
*.py[cod]                # Ignores Python bytecode files (compiled .pyc and .pyo files).

# Environment variables
.env                     # Ignores the .env file, commonly used for environment variables.
.idea                    # Ignores the .idea directory for JetBrains IDEs like PyCharm.
.venv                    # Ignores the .venv directory for Python virtual environments.

# Other files and folders to ignore
*.log                   # Ignores all log files with a .log extension.
.DS_Store                # Ignores macOS Finder metadata files.
